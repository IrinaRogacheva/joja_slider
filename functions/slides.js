function showSlide(presentation, slideIndex) {
    return currentState;
}

function moveSlide(presentation, indexes, newIndexes) {
    return presentation;
}

function deleteSlides(presentation, indexes) {
    return presentation;
}

function addSlide(presentation, slide) {
    return presentation;
}