function createPresentation() {
    return presentation;
}

function openLocalPresentation(jsonFile) {
    return presentation;
}

function openCloudPresentation(urlFile) {
    return presentation;
}

function exportPresentation(presentation) {
    return pdfFile;
}

function saveLocalPresentation(presentation) {
    return jsonFile;
}

function saveCloudPresentation(presentation) {
    return urlFile;
}

function previewPresentation(presentation) {
    return slidesList;
}
